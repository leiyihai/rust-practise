// 迷你 CLI 工具
// ===============
// 任务：整合所学知识，实现一个命令行小工具（记账/待办/随机笑话三选一）。
//
// 提示：
// - 用 std::env::args() 读取命令行参数
// - 用 match 分发不同子命令（如 add, list, remove, total）
// - 数据可以存储在 Vec 中，程序退出即清空（进阶：读写文件）
// - 考虑定义结构体表示一条记录（如 Transaction { amount, category, note }）
// - 合理使用 impl 块组织功能
// - 思考：如何处理无效输入？用 Result 还是 panic？
//
// 运行方式：cargo run --bin 07_mini_cli -- add 午饭 25
fn main() {
    
}