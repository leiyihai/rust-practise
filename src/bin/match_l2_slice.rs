// ============================================================
// 练习 10：切片/数组模式匹配 —— match_l2_slice
// ============================================================
//
// 【题目需求】
// Rust 的 match 可以对切片/数组进行模式匹配，提取首元素、尾元素等。
// 给定一个 i32 切片，用 match 识别不同长度和内容的数组模式。
//
// 【具体要求】
// 1. 准备多个切片变量：
//    let arr1 = [1, 2, 3, 4, 5].as_slice();
//    let arr2 = [42].as_slice();
//    let arr3: &[i32] = &[];
// 2. 用 match 匹配以下模式：
//    - []                  → "空数组"
//    - [single]            → "只有一个元素：{single}"
//    - [first, second]     → "两个元素：{first}, {second}"
//    - [first, .., last]   → "至少两个元素，首：{first}，尾：{last}"
//    - _                   → "其他"
// 3. 体会切片模式匹配的灵活性
//
// 【考察点】
// - 切片模式：[] 匹配空切片，[a] 匹配单元素，[a, b] 匹配双元素
// - 剩余模式 ..：忽略中间元素，类似解构中的 ..
// - [first, .., last] 匹配至少两个元素
// - 不同于 Vec，切片模式只适用于固定大小的匹配
// - 实际应用：命令行参数解析、协议消息头识别等
//
// 【代码骨架】
// fn main() {
//     let arr1 = [1, 2, 3, 4, 5].as_slice();
//     let arr2 = [42].as_slice();
//     let arr3: &[i32] = &[];
//
//     // TODO: match 不同切片，识别空、单元素、双元素、多元素模式
// }
//
// 【复习思考】
// Q: 切片模式匹配和 Vec 能用 match 吗？有什么区别？
// A: -------------------------------------------------
// Q: [first, .., last] 最少匹配几个元素？
// A: -------------------------------------------------
